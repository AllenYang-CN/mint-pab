<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allen.mapper.TagMapper">
    <resultMap id="BaseResultMap" type="com.allen.entity.Tag">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="color" property="color" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <insert id="insert" parameterType="com.allen.entity.Tag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pab_tags (name, color, created_at)
        VALUES (#{name}, #{color}, #{createdAt})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM pab_tags WHERE id = #{id}
    </delete>

    <update id="update" parameterType="com.allen.entity.Tag">
        UPDATE pab_tags
        SET name = #{name},
            color = #{color}
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT * FROM pab_tags WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM pab_tags ORDER BY id DESC
    </select>

    <select id="selectByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT * FROM pab_tags WHERE name = #{name}
    </select>
</mapper>